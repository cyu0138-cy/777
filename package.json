{
  "name": "",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "prebuild": "npm run responsive:generate && npm run responsive:check",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "responsive:generate": "node scripts/responsive-cache.mjs --generate-only",
    "responsive:check": "node scripts/responsive-cache.mjs --check-only"
  },
  "dependencies": {
    "astro": "^7.0.6",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "@types/node": "^26.1.1"
  }
}
